[build]
  command = "npm run build"
  publish = "dist"

# Owlbear Rodeo loads the manifest, pages and badge image from its own
# origin, so everything must be served with permissive CORS headers.
[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
